import styled from 'styled-components';

export const Title = styled.h1`
  color: ${(props) => props.theme.darkGold};
  font-weight: 200;
  letter-spacing: 5px;
  font-size: 32px;
  margin: 0;
  text-align: center;
`;
